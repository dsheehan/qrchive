[project]
name = "qrcodex"
version = "0.3.4"
description = "QR code management"
requires-python = ">=3.14"
dependencies = [
    "flask>=3.1.2",
    "pytest>=8.0.0",
]

[project.urls]
Repository = "https://github.com/dsheehan/qrcodex_dev"
